name: Bygg og Deploy finn-stilling-inngang
on:
    push:
    workflow_dispatch:

jobs:
    build-and-deploy:
        name: Bygg og push
        runs-on: ubuntu-latest
        permissions:
            contents: read
            id-token: write
        outputs:
            image: ${{ steps.docker-build-push.outputs.image }}
        steps:
            - uses: actions/checkout@v4
            - uses: pnpm/action-setup@v4
              with:
                  version: 12.4.1
            - uses: actions/setup-node@v4
              with:
                  node-version: 20
                  cache: 'pnpm'
                  registry-url: 'https://npm.pkg.github.com'
            - name: Installer avhengigheter
              run: pnpm install --frozen-lockfile
              env:
                  NODE_AUTH_TOKEN: ${{ secrets.READER_TOKEN }}
            - name: Bygg applikasjon
              run: pnpm run build
            - name: Installer server avhengigheter
              run: cd server && pnpm install --frozen-lockfile --prod
            - name: Push docker image to GAR
              uses: nais/docker-build-push@v0
              id: docker-build-push
              with:
                  team: toi
                  tag: ${{ github.ref == 'refs/heads/main' && 'latest' || github.sha }}

    deploy-til-dev:
        name: Deploy til dev-gcp
        needs: build-and-deploy
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/oppdater-aksel'
        runs-on: ubuntu-latest
        permissions:
            contents: read
            id-token: write
        steps:
            - uses: actions/checkout@v4
            - uses: nais/deploy/actions/deploy@v2
              env:
                  CLUSTER: dev-gcp
                  RESOURCE: .nais/app.yaml
                  VARS: .nais/dev-gcp.json
                  VAR: image=${{ needs.build-and-deploy.outputs.image }}

    deploy-til-prod:
        name: Deploy til prod-gcp
        needs: build-and-deploy
        if: github.ref == 'refs/heads/main'
        runs-on: ubuntu-latest
        permissions:
            contents: read
            id-token: write
        steps:
            - uses: actions/checkout@v4
            - uses: nais/deploy/actions/deploy@v2
              env:
                  CLUSTER: prod-gcp
                  RESOURCE: .nais/app.yaml
                  VARS: .nais/prod-gcp.json
                  VAR: image=${{ needs.build-and-deploy.outputs.image }}
